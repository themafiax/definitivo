android {
    namespace = "com.example.flutter_definitivo"
    compileSdk = 34  // Cambia esto a la última versión estable

    ndkVersion = "25.1.8937393"  // Asegúrate de tener esta versión instalada

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17  // Usa JDK 17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.example.flutter_definitivo"
        minSdk = 21  // Puedes ajustarlo según tu necesidad
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}
